__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.eggs/
*.egg
.pytest_cache/
.mypy_cache/
.tox/
.venv/
venv/
*.so
.coverage
htmlcov/
.sworn/keys/active.key
.sworn/signing.key

# auto: spec 151 — portfolio trivial gitignore sweep
release/
release/evidence/autopilot_*/
CRAFT_GATE_RESULT.json

# Release evidence is regenerated by scripts/release_phase0_readiness.sh and
# carries absolute /Users/... paths plus a full environment inventory. It was
# removed from the published lineage by a prior scrub; without this rule a
# re-run stages it again and re-publishes exactly what that scrub removed.
release-evidence/
.venv-release/
.lineage-state/
